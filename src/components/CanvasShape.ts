import { FC } from "react";
import { Segment } from "../types/Segment";

const CanvasShape: FC<{
  segments: Segment[],
  fill: string;
  stroke: string;
  strokeWidth: number;
}> = () => {
  return null;
};

export default CanvasShape;
